# Custom Challenge Prompts

Drop a `.txt` file here named after the challenge slug to use a custom system prompt.

Example: `sales.txt` will be used instead of the built-in prompt for the "sales" challenge.

The prompt is the system message sent to the LLM before each turn of the conversation.
